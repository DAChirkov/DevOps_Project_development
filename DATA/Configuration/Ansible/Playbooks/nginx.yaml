- name: Installation and configuration Nginx
  gather_facts: true
  become: true
  serial: 5
  hosts: nginx
  remote_user: azroot

  tasks:
    - name: Installing the Nginx package
      apt:
        name:
        - nginx
        state: present
        update_cache: yes
    
    - name: Сopying