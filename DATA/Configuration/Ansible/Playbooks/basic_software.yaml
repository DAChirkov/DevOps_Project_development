- name: Installing basic software
  gather_facts: true
  become: true
  serial: 5
  hosts: all
  remote_user: azroot

  tasks:
    - name: Installing packages
      apt:
        name:
        - mc
        - htop
        - tree
        state: present
        update_cache: yes